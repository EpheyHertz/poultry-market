// This file is intentionally minimal — homepage metadata comes from app/layout.tsx
// The root layout's metadata applies to the homepage (app/page.tsx is a client component).
// The canonical for the homepage is set via seoConfig.alternates.canonical = SITE_URL.
export {};
